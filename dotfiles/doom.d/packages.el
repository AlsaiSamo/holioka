(package! exec-path-from-shell)
(package! pinentry)

(package! org-alert)
;; (package! org-notify)
;; TODO: cannot pin because nix-straight intercepts the package and always inserts old one
(package! org-clock-reminder
  ;; :recipe
  ;; (:host github
  ;;  :repo "inickey/org-clock-reminder"
  ;;  )
  ;; :pin "a56cedc42c55")
  )
(package! alert)
;; TODO: org-random-todo or something similar

(package! org-bullets)
